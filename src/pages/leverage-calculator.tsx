import Head from "next/head"

// eslint-disable-next-line import/no-default-export
export default (): JSX.Element => (
  <div className="container">
    <Head>
      <title>Create Next App</title>
    </Head>

    <div>hello LeverageCalculator</div>
  </div>
)
